# Pull request guidance

<!--
Copy of the upsert-pr template. Write this to `.claude/upsert-pr.md` in the
repository root, with the user's agreement.

Keep every entry short. This is guidance for a capable model, not a procedure.
Delete a heading rather than fill it with something obvious or invented. See
`example-guidance.md` beside the skill for a filled one.

Delete this comment in the file you write.
-->

## Base branch

<!--
Only when it is not the repository default. Add the merge style if it changes
how the title is written, for example squash and merge, where the title becomes
the commit message.
-->

## Title

<!--
The format, taken from the history of merged titles. A table of prefixes if the
repository uses them, one line per prefix, saying when to use it. Anything that
must never appear in a title.
-->

## Issue tracker

<!--
Which tracker, and how the branch finds the issue, for example an identifier in
the branch name. How to confirm the issue exists, and how to link it. What to do
when the branch carries no identifier.
-->

## Verification

<!--
The commands that prove the change, and the directory to run them from. The
skill runs them and reports the true result. List only commands that are quick
and safe to run.
-->

## Template notes

<!--
One bullet per section of `.github/pull_request_template.md` that needs a rule
the section comment does not already give. Sections to delete when empty,
sections the model cannot fill, and how many boxes to check. Skip the sections
that are self-explanatory.
-->
